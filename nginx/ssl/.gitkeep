# Keep this directory tracked so the docker-compose bind mount
# (./nginx/ssl:/etc/nginx/ssl) has a mount point on a fresh clone.
# The .pem files here are generated by scripts/generate-ssl.sh and gitignored.
